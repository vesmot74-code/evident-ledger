# Sprint B1+B2 Report

**Date:** 2026-08-16  
**Parent audit:** [`docs/audits/2026-08-15-product-readiness-audit.md`](2026-08-15-product-readiness-audit.md) (Sprint A — product readiness, production-backed loop)  
**Base commit:** `170e9171` (`chore(dev): add deterministic local development entrypoints`)  
**Branch:** `main`  
**Mode:** Diagnose-first; code changes only if B1(b)/(c) or B2 field-source bugs confirmed  
**Artifact workspace:** `/tmp/b1-local-audit/`

### Pre-flight (required)

| Check | Result |
| --- | --- |
| `git status --short` | `?? docs/audits/2026-08-15-product-readiness-audit.md` (Sprint A report only; no product code dirty) |
| `git branch --show-current` | `main` |
| `git log --oneline -5` | `170e9171` … `7f8e389a` |

---

## B1 — Root cause

**Confirmed: (a) — expected environment separation.**

| Hypothesis | Result | Evidence |
| --- | --- | --- |
| **(a)** Production API key not present in local Docker DB | **CONFIRMED** | `EVIDENT_SERVER_URL=http://127.0.0.1:3000` + existing `~/.evident/api_key` → `account info` **REJECTED**; `new-chain` → `API key rejected`. Key was issued against production (`evident-ledger.com`), not local `ledger` DB. |
| **(b)** CLI cannot target local API | **CLOSED — no defect** | Existing override works: `src/bin/evident.rs` `server_url()` → `EVIDENT_SERVER_URL` with fallback `https://evident-ledger.com`. With override + **local** key, CLI talks to localhost successfully. **No CLI code change.** |
| **(c)** Auth logic bug rejecting valid local keys | **Not indicated** | Fresh key from `POST /accounts/register` on localhost → `account info` **ACTIVE / FREE**; `new-chain` + `commit` succeed. |

**Clarifications (TZ corrections applied):**

- `DATABASE_URL` = which DB the **server** uses; `EVIDENT_SERVER_URL` = where the **CLI** sends HTTP. No mapping between them was attempted.
- Bare `evident account` can print `Plan: UNKNOWN` without surfacing HTTP reject; use `account info` for auth status.
- Certificate QR remains canonical `https://evident-ledger.com/public/verify/{pv_…}/certificate.pdf` even when the loop ran on localhost — **not treated as a B1 defect**; local verification API ≠ public QR target by design for this sprint.

**Code changes for B1: NONE.**

---

## B1 — Local loop result

Local key: `/tmp/b1-dev-api-key` (chmod 600; does **not** overwrite `~/.evident/api_key`).  
Env for CLI: `EVIDENT_SERVER_URL=http://127.0.0.1:3000` + `EVIDENT_API_KEY=$(cat /tmp/b1-dev-api-key)`.

| Stage | Result |
| --- | --- |
| dev-server start | **PASS** — `HTTP 200` on `http://127.0.0.1:3000/` (binary `evident-ledger` + Docker `db`; note: process must stay alive across shell sessions) |
| API key | **PASS** — local register → ACTIVE/FREE; production home key correctly REJECTED on local (a) |
| project / chain | **PASS** — `new-chain` → `90af7958-613c-465b-9680-9b9660833929` |
| commit / anchor | **PASS** — event `70c753bc-ca7a-4686-85a1-dba9c842f4a9`, Machine TSA, Trust Level Standard |
| Certificate | **PASS** — Tier-1 PDF `/tmp/b1-local-audit/Evident-Certificate.pdf` (43081 bytes, 3 pages) via same `generate_file_certificate` harness as Sprint A |
| QR | **PASS** — decoded `https://evident-ledger.com/public/verify/pv_wRNMVfo4E7uNvZSjaoq2P/certificate.pdf` (canonical public URL; local API used separately) |
| Public Verify (local API) | **PASS** — `GET /public/verify?file_hash=…` → `exists:true`, `integrity:VALID`, `tsa_class:basic`, `pv_wRNMVfo4E7uNvZSjaoq2P`; existence PDF HTTP 200 |
| Offline Verify | **PASS** — `verify --event/--chain` exit 0, Verification Result PASS, TSA CONFIRMED, Lifecycle CERTIFIED |
| Tamper Test (file hash) | **PASS** — original `bba97aa6…` ≠ tampered `d473a1bb…` → INVALID vs Certificate/evidence SHA-256 |

### Local fixture IDs

| Field | Value |
| --- | --- |
| SHA-256 | `bba97aa66f7bda7036749fc7cdee56f7946ba2c86f08f500076b2b35c60055b8` |
| chain_id | `90af7958-613c-465b-9680-9b9660833929` |
| event_id | `70c753bc-ca7a-4686-85a1-dba9c842f4a9` |
| certificate_id | `cert_bd04d2aa32065f58b576ea22d8fba267` |
| public_proof_id | `pv_wRNMVfo4E7uNvZSjaoq2P` |
| evidence | `~/.evident/evidence/ev_70c753bcca7a468685a1dba9c842f4a9.json` |
| proof | `~/.evident/proofs/90af7958-…/70c753bc-….json` |

---

## B2 — Consistency table (local)

Regression on **local** Certificate + local Public Verify (not production Sprint A artifact). Compared against Sprint A STEP 5 disclosure contract.

| Field | Certificate (Tier-1) | Public Verify | Match |
| --- | --- | --- | --- |
| SHA-256 | `bba97aa66f7bda7036749fc7cdee56f7946ba2c86f08f500076b2b35c60055b8` | not disclosed | N/A-by-design |
| Proof ID | in QR only (`pv_wRNMVfo4E7uNvZSjaoq2P`) | `pv_wRNMVfo4E7uNvZSjaoq2P` | **PASS** (QR ↔ API) |
| Chain ID | `90af7958-613c-465b-9680-9b9660833929` | not disclosed | N/A-by-design |
| Event ID | `70c753bc-ca7a-4686-85a1-dba9c842f4a9` | not disclosed | N/A-by-design |
| Timestamp | Registered At `2026-08-15 19:32:41 UTC` (`EvidenceRecord.registered_at`) | `2026-08-15T19:32:40.641005+00:00` (registry) | **PARTIAL** — ~0.85s skew between client record clock and public registry insert; same registration, different layers |
| Signature | Ed25519 present | not disclosed | N/A-by-design |
| TSA | `CONFIRMED` + serial `tsr-1786822361` | class=`basic` | **PARTIAL** (class only publicly) |
| Status | `CERTIFIED` / lifecycle | `VALID` / PDF `REGISTERED` | **PARTIAL** (different vocab, both positive) |
| Scope of Attestation wording | Full Tier-1 scope block | Short existence statement | N/A-by-design (different documents) |

**B2 verdict:** No Certificate↔Public field-source bug of the “two writers for one canonical value” class. Disclosure asymmetry matches Sprint A. Timestamp skew noted; **no code fix** in this sprint (not a false attestation; not a cached-report class bug).

---

## Changes made (if any)

**NONE** — B1 root cause (a); B1(b) already satisfied by `EVIDENT_SERVER_URL`; B2 no actionable field-source sync bug.

Validation gates (`cargo check` / `build` / `test`) **not re-run** — no product code modified.

---

## NOT committed — awaiting review

```text
git status --short
?? docs/audits/2026-08-15-product-readiness-audit.md
?? docs/audits/2026-08-16-sprint-b1-b2.md
```

`git --no-pager diff` (tracked files): **empty**.

No `git add` / `git commit` / `git push` performed.

---

## Observed (not fixed — STOP)

| Item | Note |
| --- | --- |
| Local server process lifetime | `./scripts/dev-server.sh` / `cargo run` often dies when the spawning tool session ends; audit used direct `nohup` on built `evident-ledger` binary. Operational note for B3/docs — not a product auth bug. |
| QR always production host | Documented as expected for B1; do not change Certificate renderer for local-only URL. |
| B3 ZIP / B4 GUI | Not started. |

---

## Recommended next (B3)

Sprint B3 — Advanced Evidence Package ZIP: exercise штатный GUI ZIP path (or document CLI-only substitute), verify package contents vs Certificate / proof contract. Do not mix with B4 GUI UX cleanup.

---

## STOP CONDITION

B1+B2 complete. No product code changes. Awaiting human review before any commit of audit markdown (or future fixes).
