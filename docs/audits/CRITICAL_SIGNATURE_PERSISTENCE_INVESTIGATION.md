# Critical: signature persistence investigation

Date: 2026-07-23  
Status: **Root cause confirmed — FIXED** (`fix(ledger): persist signature on legacy /events path`)  
Environment (diagnosis): live pilot process pid 4500, DB `evident_ledger_pilot_11_6`

---

## Root cause

CLI `evident commit` calls **`POST /events`** (legacy), **not** `POST /v1/events`.

| Layer | Code |
|---|---|
| CLI | `src/client.rs` → `POST {base_url}/events` |
| Router | `src/main.rs` → `.nest("/events", api::events::router(…))` |
| Handler | `src/api/events.rs` → `submit_event(…)` |
| Ledger | `src/service/ledger.rs` |

### Mechanism (pre-fix)

1. **`insert_event_in_tx`** inserted with hardcoded empty signature (`.bind("")`).
2. **`submit_event`** committed **before** signing.
3. **`finalize_event_submission`** signed only into the HTTP JSON response.
4. No `persist_event_signature` on the legacy path.

Asymmetry introduced in **`bb43af7`** (v1 persist only).

### Fix applied

`submit_event` now: insert placeholder → `proof_snapshot_at_event` + **`persist_event_signature`** (same helpers as v1) → **`tx.commit()`** → if `ProofStatus::Anchored`, `on_proof_anchored` → finalize response using the **same** snapshot signature bytes (response == DB).

---

## Path comparison (`/events` vs `/v1/events`) after fix

| Step | Legacy `POST /events` | `POST /v1/events` |
|---|---|---|
| Insert placeholder `signature=""` | Yes (`insert_event_in_tx`) | Yes (shared) |
| Server signature | `proof_snapshot_at_event` / `ServerSigner::sign_root` | Same helpers |
| Persist signature | `persist_event_signature` **before** commit | Same |
| Identity signature / PoP | Optional columns on `SubmitEventRequest`; **no** PoP challenge validation on this path | Validated via `IdentitySigningService` when present |
| Commit | After persist | After persist + idempotency record |
| Public materialization | `on_proof_anchored` if `Anchored` (same gate) | Same gate via `materialize_public_proof_after_anchor` |
| TSA | `stamp_chain` after commit in finalize | `post_commit_tsa` after commit |
| Client JSON | Full proof object (CLI) | Compact `{event_id, proof_status, …}` |

**ServerSigner:** both handlers receive `state.signer.as_ref()` from the single `Arc<ServerSigner>` loaded in `main.rs` via `ServerSigner::load_or_create(&config.signing_key_path)`.  
Confirmed: **legacy and v1 signature bytes are generated by the same ServerSigner instance/path.**

---

## Remaining divergences (not fixed in this commit)

| ID | Finding | Severity | Notes |
|---|---|---|---|
| D1 | Identity PoP: v1 validates; legacy can accept pre-filled identity columns without challenge | Medium | CLI does not send identity; API callers of `/events` differ from `/v1/events` |
| D2 | Response schema differs (full proof vs compact v1) | Info | Intentional for CLI |
| D3 | Legacy idempotency cache return omits proof payload | Low | Pre-existing; separate from signature persist |
| D4 | Qualified TSA still blocks paid-plan writes | Medium (known) | Unrelated; Stage 11.6 |

---

## Scope of impact (pre-fix)

| Question | Answer |
|---|---|
| Broken path | Legacy `POST /events` (CLI) |
| `/v1/events` | Correct since `bb43af7` |
| Pilot DB | 6/6 empty before fix (test data; truncate after verification) |

### Verification query

```sql
SELECT event_id, chain_id, sequence, created_at,
       (signature = '' OR signature IS NULL) AS signature_missing
FROM events
ORDER BY created_at DESC;
```

---

## Severity confirmation

Was **CRITICAL**. Closed by persist-before-commit on legacy path + regression tests in `tests/legacy_events_signature_persist.rs`.

---

## Perimeter check: legacy `/events` vs `/v1/events`

Date: 2026-07-23 (post-fix code review; **no code changes** in this section)  
Confirmed smoke (operator): `event_id=49d0fcdf-66c9-4f41-8e07-ac49c3c37e42` — `signature` len 128, `proof_status=anchored`, TSA `tsr-1784800586`, registry `pv_A4FoZX6wgq4NALbUmfXc9C` REGISTERED / VALID.

Legend for **Status**: `OK (shared)` = same function / equivalent behavior · `OK (intentional)` = different by design · `GAP` = divergence with residual risk.

| Stage | v1 `/events` | legacy `/events` | Status |
|---|---|---|---|
| request validation | `validate_submit_request`: `event_type`, `file_hash` format; `event_id` required if identity present; `Idempotency-Key` header required | Axum JSON → `SubmitEventRequest` only; **no** `is_valid_file_hash` / event_type checks; `parent_event_id` accepted but **ignored** at insert (head from chain) | **GAP — Low**: weaker input validation on legacy; CLI always sends well-formed SHA-256 |
| capability checks | `get_account_capabilities` for trust/tsa_class; `require_feature(Identity)` when identity present; commit/TSA limits inside shared `insert_event_in_tx` | No Identity `require_feature`; commit/TSA/`tsa_available` via **same** `insert_event_in_tx` | **GAP — Medium** on Identity entitlement (see identity fields); tariff/TSA limits **OK (shared)** |
| idempotency | Header key → `idempotency_records` (+ `request_hash` conflict); full response replay | Body `idempotency_key` → lookup on `events` row; replay returns `{event_id, cached:true}` **without** proof | **GAP — Low/Medium**: dual stores; legacy replay omits proof (D3) |
| insert | Shared `insert_event_in_tx` | Shared `insert_event_in_tx` | **OK (shared)** |
| placeholder signature | `.bind("")` then overwrite | Same | **OK (shared)** |
| server signature | `proof_snapshot_at_event` → `ServerSigner::sign_root` via `state.signer` | Same helpers + same `state.signer` | **OK (shared)** |
| persist signature | `persist_event_signature` before commit | Same, before commit | **OK (shared)** — critical gap closed |
| identity fields | `IdentitySigningService::validate_and_prepare` (key ownership, not revoked, Ed25519 over leaf hash) then bind verified columns | Rejects any of `identity_key_id` / `identity_signature` / `identity_fingerprint` with **400** (`IdentityNotSupportedOnLegacyPath`); Identity only via `/v1/events` | **OK (intentional)** — P1 closed (Option A); see [P1_LEGACY_EVENTS_IDENTITY_FIELDS.md](P1_LEGACY_EVENTS_IDENTITY_FIELDS.md) |
| commit | After persist + idempotency row | After persist | **OK** (ordering correct on both) |
| proof status | `derive_proof_status` → returned in JSON | `derive_proof_status` used for materialization gate only; not in legacy JSON | **OK (intentional)** for CLI; status readable via `GET /v1/proof` |
| materialization | `on_proof_anchored` if `Anchored` (wrapper) | Direct `on_proof_anchored` if `Anchored` | **OK (shared function)** |
| TSA | `post_commit_tsa` → `stamp_chain` after commit | `finalize_event_submission` → `stamp_chain` after commit | **OK (equivalent)** |
| response | Compact `{event_id, proof_status, trust_level, …}` | Full proof + events + TSA for CLI | **OK (intentional)** |
| subscription enforcement | Same `subscription_enforcement_middleware` on `/v1` nest | Same middleware on `/events` router | **OK (shared)** |

### Detail notes

1. **ServerSigner** — single `Arc` from `main.rs` `load_or_create(SIGNING_KEY_PATH)`; both paths `state.signer.as_ref()`. Legacy and v1 signature bytes are generated by the **same ServerSigner instance/path**.

2. **Identity (items 8–9)** — only v1 calls `require_feature(Feature::Identity)` and cryptographic PoP validation. Legacy now **rejects** identity-bearing bodies at the handler boundary (does not silently drop). CLI does not send identity fields.

3. **Subscription (item 15)** — shared middleware; Stage 11.3 parity confirmed. No new discrepancy.

4. **File hash normalization** — v1 lowercases via `normalized_file_hash`; legacy stores request string as-is. CLI emits lowercase hex → OK for pilot CLI; mixed-case API callers of `/events` could diverge from v1 registry keys (**Low**).

### Perimeter verdict

**No further Critical or High discrepancies found between legacy and v1 paths** for the default CLI write path (server signature, persist, commit order, materialization, TSA, subscription enforcement).

**Open / residual (post-P1):**

| ID | Finding | Severity | Status |
|---|---|---|---|
| P1 | Legacy `/events` accepts/persists identity columns without `require_feature` or PoP validation | Medium (High candidate) | **Resolved** — Option A reject-on-legacy; [P1_LEGACY_EVENTS_IDENTITY_FIELDS.md](P1_LEGACY_EVENTS_IDENTITY_FIELDS.md) |
| P2 | Dual idempotency models; legacy cache response lacks proof | Low–Medium | Open |
| P3 | Weaker request validation / no file_hash normalize on legacy | Low | Open |
